# Out of Scope

已審視並明確拒絕的機制。每個檔案是一個**概念**（不是一次提案）：記錄為什麼不做、先例在哪、以及既有的替代出口。

用法：

- 提案任何新機制前，先按**概念相似度**（不是關鍵字）比對本目錄；命中就先讀該檔。
- 只有「審視後拒絕」的概念才進來；已被別的機制實作的東西不算拒絕，不得寫入（會污染判斷）。
- 若情勢改變要翻案：在對應檔補上新論據後再提案，不要無視既有結論重新開題。

格式：`<concept-slug>.md`，內含「## 為什麼不做」與「## 先例」，有替代出口時加「## 替代出口」。
